<div class="top-row ps-2 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Penske Sample</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-2">
            <NavLink class="nav-link" href="nascarSchedule">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Nascar Schedule Grid
            </NavLink>
        </div>
        <div class="nav-item px-2">
            <NavLink class="nav-link" href="nascarScheduleCards">
                <span class="oi oi-list-rich" aria-hidden="true"></span>Nascar Schedule Cards
            </NavLink>
        </div>
        <div class="nav-item px-2">
            <NavLink class="nav-link" href="swagger">
                <span class="oi oi-list-rich" aria-hidden="true"></span>Swagger
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
